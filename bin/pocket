#!/bin/bash
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
export PYTHONIOENCODING=utf-8
export PYTHONCOERCECLOCALE=1
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
PYTHONPATH="$SCRIPT_DIR:$PYTHONPATH" python3 -X utf8 -m pocketclaw "$@"
